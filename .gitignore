# ---- large data cubes (download separately — see README) ----
Datasets/*.nc

# ---- third-party copyrighted publication ----
Modeled Particle Retention for Habitat Prediction publication.pdf

# ---- in-progress paper drafts (kept private) ----
paper.tex
paper_climate.md

# ---- pretrained TabICL weights (110 MB, over GitHub's file limit) ----
# Auto-downloaded from the HF Hub on first use, or pass --model-path.
models/

# ---- editor / office backup files ----
*.bak

# ---- derived retention caches (regenerated from the .nc cubes) ----
figures/**/.retention*.npy
.retention*.npy

# ---- python / tooling ----
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.claude/

# ---- stray / environment ----
.env
*.sqlite-journal

# ---- Lavish review artifacts (local scratch, not part of the project) ----
.lavish/

# ---- Colab runtime artifacts ----
vllm.log
tunnel.log
cloudflared

# NOTE: figures/**/cells_export.npz IS committed on purpose (< 1 MB each) —
# it is the frozen CV split the Modal fine-tuning run scores against.

# NOTE: figures/**/tabllm_cache.sqlite is intentionally committed
# (it is the LLM-reproducibility anchor; LLM output is not byte-deterministic).
